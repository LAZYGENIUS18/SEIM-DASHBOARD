# SEIM-DASHBOARD
Full-stack SIEM dashboard that executes real security tools (Nmap, Nuclei, ffuf, Subfinder, etc.) via subprocess and streams live results over WebSocket into a dark-themed SOC console. Flask + SQLite backend, vanilla JS + Chart.js frontend.
